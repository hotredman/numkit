#pragma once
// integration.hpp moved to the runtime layer
// (numkit/runtime/math/integration/integration.hpp) as Phase 3-A step C3 —
// integral/quad compute pulls core/engine, so it is core-coupled. Forwarding
// stub; ns still numkit::builtin (rename is C4).
#include <numkit/runtime/math/integration/integration.hpp>
