#pragma once
// group.hpp moved to the runtime layer (numkit/runtime/math/group/group.hpp) as
// Phase 3-A step C3 — group* (groupsummary/findgroups...) compute pulls
// core/engine, so it is core-coupled. Forwarding stub; ns still numkit::builtin
// (rename is C4).
#include <numkit/runtime/math/group/group.hpp>
