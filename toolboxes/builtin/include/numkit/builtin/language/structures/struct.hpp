#pragma once
// struct.hpp moved to the runtime layer
// (numkit/runtime/language/structures/struct.hpp) as Phase 3-A step C3 — struct
// compute pulls core/engine + vm + callback_builtin, so it is core-coupled.
// Forwarding stub; ns still numkit::builtin (rename is C4).
#include <numkit/runtime/language/structures/struct.hpp>
