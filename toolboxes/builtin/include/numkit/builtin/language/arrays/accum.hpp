#pragma once
// accum.hpp moved to the runtime layer (numkit/runtime/language/arrays/accum.hpp)
// as Phase 3-A step C3 — accumarray's compute pulls core/engine, so it is
// core-coupled and belongs in runtime. Forwarding stub; ns still numkit::builtin
// (rename is C4).
#include <numkit/runtime/language/arrays/accum.hpp>
