#pragma once
// containers.hpp moved to the runtime layer (numkit/runtime/containers.hpp) as
// Phase 3-A step C3 — containers (containers.Map) compute pulls core/engine, so
// it is core-coupled. Forwarding stub; ns still numkit::builtin (rename is C4).
#include <numkit/runtime/containers.hpp>
