#pragma once
// var_reduction.hpp moved to math/src/arithmetic/ (Phase 3-A step C1). Forwarding
// stub so cross-toolbox consumers that include "math/arithmetic/var_reduction.hpp"
// via the builtin/src -I root (toolboxes/stats/src/descriptive/*, and the
// relocated reductions.cpp itself) keep building until the C4 cleanup.
#include "../../../../../math/src/arithmetic/var_reduction.hpp"
