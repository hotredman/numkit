#pragma once
// cumsum.hpp moved to math/src/arithmetic/ (Phase 3-A step C1). Forwarding stub
// so cross-toolbox consumers that include "math/arithmetic/cumsum.hpp" via the
// builtin/src -I root (e.g. toolboxes/builtin/src/language/arrays/matrix.cpp)
// keep building until the C4 cleanup retargets them.
#include "../../../../../math/src/arithmetic/cumsum.hpp"
