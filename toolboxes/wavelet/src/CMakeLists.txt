set(WAVELETLIB_SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/library.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/filter/wfilters.cpp      # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/filter/wfilters_reg.cpp  # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/filter/qmf.cpp           # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/filter/qmf_reg.cpp       # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/filter/families.cpp      # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/filter/families_reg.cpp  # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/dwt.cpp           # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/dwt_reg.cpp       # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/multilevel.cpp   # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/multilevel_reg.cpp  # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/dwt2.cpp          # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/dwt2_reg.cpp      # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/dyad.cpp          # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/dyad_reg.cpp      # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/wkeep_wextend.cpp     # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/wkeep_wextend_reg.cpp # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/haart.cpp        # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/ihaart.cpp       # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/haar_reg.cpp     # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/wrcoef.cpp       # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/dwt/wrcoef_reg.cpp   # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/denoise/denoise.cpp      # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/denoise/denoise_reg.cpp  # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/shape/shape.cpp          # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/shape/shape_reg.cpp      # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/shape/gauss.cpp          # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/shape/gauss_reg.cpp      # register (CallContext)
    ${CMAKE_CURRENT_SOURCE_DIR}/swt/swt.cpp              # compute (engine-free)
    ${CMAKE_CURRENT_SOURCE_DIR}/swt/swt_reg.cpp          # register (CallContext)
)

set(WAVELETLIB_SOURCES ${WAVELETLIB_SOURCES} PARENT_SCOPE)
