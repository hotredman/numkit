{
  "name": "return",
  "namespace": "builtin",
  "setup": "",
  "expr": "r1 = 1;",
  "iters": 1,
  "tol": 1e-12,
  "fingerprint": ["r1"],
  "comment": "DEFERRED — script-level `return` causes MATLAB's `run` wrapper to error (stops the wrapper); numkit allows it. Cannot express in single-snippet parity spec. Functionality validated by gtest. Placeholder spec; KNOWN GAP
}
