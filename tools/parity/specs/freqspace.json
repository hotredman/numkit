{
  "name": "freqspace",
  "namespace": "image",
  "setup": "[f1, f2] = freqspace([4 6]);",
  "expr": "y = [f1, NaN, f2];",
  "iters": 5,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: [f1, f2] = freqspace(N|[N M]) or f = freqspace(N[, 'whole']). Now supports 2-output centered form and 2-vec [N M] input (via libs/builtin extension)."
}
