{
  "name": "isletter",
  "namespace": "core",
  "setup": "s = repmat('abc123 def456 ', 1, 1000);",
  "expr": "y = isletter(s);",
  "iters": 1000,
  "tol": 1e-10,
  "comment": "Sig: TF = isletter(S). 14k char input. 1000 iters. Logical-array fp.",
  "fingerprint": [
    "sum(double(y(:)))",
    "double(y(1))",
    "double(y(end))"
  ]
}
