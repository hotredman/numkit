{
  "name": "filt",
  "namespace": "control",
  "setup": "f = filt([1 -0.5], [1 -0.7]);",
  "expr": "[n, d] = tfdata(f, 'v');",
  "iters": 5,
  "tol": 1e-12,
  "fingerprint": [
    "f.Ts",
    "n(1)",
    "n(2)",
    "d(1)",
    "d(2)"
  ],
  "comment": "Sig: f = filt(num, den[, Ts]). Discrete tf with z^-1 ordering convention. Default Ts = -1 (unspecified discrete). Internally same coefficients as tf(num, den, Ts) — the variable convention only affects display."
}
