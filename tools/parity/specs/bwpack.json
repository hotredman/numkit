{
  "name": "bwpack",
  "namespace": "image",
  "setup": "BW = eye(5);",
  "expr": "y = bwpack(BW);",
  "iters": 5,
  "tol": 0,
  "out_var": "y",
  "comment": "Sig: BWP = bwpack(BW). Pack 32 binary rows into one uint32 (LSB = row 0). Octave-image has bwpack."
}
