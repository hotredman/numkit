{
  "name": "deltaE",
  "namespace": "image",
  "setup": "A = reshape(linspace(20, 80, 12), 4, 3); B = reshape(linspace(50, 30, 12), 4, 3);",
  "expr": "y = deltaE(A, B, 'isInputLab', true);",
  "iters": 5,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: delE = deltaE(I1, I2[, 'isInputLab', tf]). CIE76 distance in CIELAB. We test the Lab-input path (skips rgb2lab to avoid the known sRGB-vs-linear divergence between numkit and MATLAB)."
}
