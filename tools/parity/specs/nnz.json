{
  "name": "nnz",
  "namespace": "core",
  "setup": "x = mod(1:1000000, 13) == 0;",
  "expr": "y = nnz(x);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: N = nnz(X). 1M-pt count. 1000 iters.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
