{
  "name": "goertzel",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 2*pi*10, 1024));",
  "expr": "y = goertzel(x, 10:50);",
  "iters": 100,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: Y = goertzel(X, F). 41 freq bins. 100 iters."
}
