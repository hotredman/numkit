{
  "name": "mpower",
  "namespace": "builtin",
  "setup": "",
  "expr": "r1 = mpower(2, 3); r2 = mpower(5, 2); r3 = mpower(3, 0);",
  "iters": 5,
  "tol": 1e-12,
  "fingerprint": [
    "r1",
    "r2",
    "r3"
  ],
  "comment": "Sig: r = mpower(a,b) (a^b). Spec-extension batch 2026-05-09. KNOWN GAP: matrix^integer (M^n where M is matrix) not implemented in numkit \u2014 only scalar^scalar pinned. Documented as separate \u0422\u0417; would need O(log n) repeated mtimes for the matrix branch."
}
