{
  "name": "optimget",
  "namespace": "core",
  "setup": "o = optimset('TolX',1e-6);",
  "expr": "y = optimget(o, 'TolX');",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: V = optimget(O, NAME). 10000 iters.",
  "fingerprint": [
    "double(y)"
  ]
}
