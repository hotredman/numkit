{
  "name": "sawtooth",
  "namespace": "core",
  "setup": "t = linspace(0, 4*pi, 10000);",
  "expr": "y = sawtooth(t);",
  "iters": 1000,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: Y = sawtooth(T). 1000 iters."
}
