{
  "name": "sin",
  "namespace": "core",
  "setup": "x = linspace(-2*pi, 2*pi, 1000000);",
  "expr": "y = sin(x);",
  "iters": 20,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: Y = sin(X). 1M-point sweep over 4π. 20 iters. Element-wise SAVE."
}
