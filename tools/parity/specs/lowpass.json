{
  "name": "lowpass",
  "namespace": "core",
  "setup": "fs = 1000; x = sin(linspace(0, 100, 10000));",
  "expr": "y = lowpass(x, 100, fs);",
  "iters": 100,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: Y = lowpass(X, FPASS, FS). 10k pts, 100 Hz cutoff at fs=1k. 100 iters."
}
