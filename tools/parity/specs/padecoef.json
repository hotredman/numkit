{
  "name": "padecoef",
  "namespace": "core",
  "setup": "T = 1.5; N = 10;",
  "expr": "[num, den] = padecoef(T, N);",
  "iters": 10000,
  "tol": 1e-12,
  "fingerprint": ["sum(num)", "sum(den)", "num(1)", "den(1)"],
  "out_var": "num",
  "comment": "Pade(10,10) of e^{-1.5s} numerator coefficients. 10k iters. Octave's padecoef (control pkg) uses a different normalization — comparison reference is MATLAB."
}
