{
  "name": "matches",
  "namespace": "core",
  "setup": "s = repmat('apple_banana_cherry_', 1, 10); pat = 'banana';",
  "expr": "y = matches(s, pat);",
  "iters": 1000,
  "tol": 1e-09,
  "comment": "Sig: TF = matches(S, PAT). Single string check. 10000 iters.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
