{
  "name": "image_bwmorph",
  "namespace": "image",
  "setup": "rng(0); BW = rand(20, 20) > 0.5;",
  "expr": "j_dil = sum(sum(bwmorph(BW, 'dilate'))); j_ero = sum(sum(bwmorph(BW, 'erode'))); j_opn = sum(sum(bwmorph(BW, 'open'))); j_cls = sum(sum(bwmorph(BW, 'close'))); j_brd = sum(sum(bwmorph(BW, 'bridge'))); j_cln = sum(sum(bwmorph(BW, 'clean'))); j_dia = sum(sum(bwmorph(BW, 'diag'))); j_fil = sum(sum(bwmorph(BW, 'fill'))); j_hbr = sum(sum(bwmorph(BW, 'hbreak'))); j_maj = sum(sum(bwmorph(BW, 'majority'))); j_p4 = sum(sum(bwmorph(BW, 'perim4'))); j_p8 = sum(sum(bwmorph(BW, 'perim8'))); j_rem = sum(sum(bwmorph(BW, 'remove'))); j_end = sum(sum(bwmorph(BW, 'endpoints'))); j_fat = sum(sum(bwmorph(BW, 'fatten'))); j_bot = sum(sum(bwmorph(BW, 'bothat'))); j_top = sum(sum(bwmorph(BW, 'tophat'))); j_skel = sum(sum(bwmorph(BW, 'skel', Inf))); j_thin = sum(sum(bwmorph(BW, 'thin', Inf))); j_thick = sum(sum(bwmorph(BW, 'thicken', 3))); j_spur = sum(sum(bwmorph(BW, 'spur', 5))); j_shr = sum(sum(bwmorph(BW, 'shrink', Inf))); j_bp = sum(sum(bwmorph(BW, 'branchpoints')));",
  "iters": 5,
  "tol": 0,
  "fingerprint": ["j_dil", "j_ero", "j_opn", "j_cls", "j_brd", "j_cln", "j_dia", "j_fil", "j_hbr", "j_maj", "j_p4", "j_p8", "j_rem", "j_end", "j_fat", "j_bot", "j_top", "j_skel", "j_thin", "j_thick", "j_spur", "j_shr", "j_bp"],
  "comment": "MATLAB Image Toolbox bwmorph — binary morphological operations. Faithful port of MATLAB R2025b bwmorph.m + algbwmorph.m: each operation is a 3×3 neighbourhood LUT lookup (or chain of lookups with bitwise compositions) using MATLAB's makelut bit convention (bit k = neighbour((k%3)-1, (k/3)-1) relative to centre). The 14 base LUTs (lutdilate, luterode, lutbridge, lutclean, lutdiag, lutendpoints, lutfatten, lutfill, luthbreak, lutmajority, lutper4, lutper8, lutremove, lutbranchpoints) plus 8 skeleton sub-LUTs (lutskel1..8) and the support tables (lutshrink, lutsingle, lutspur, lutthin1, lutthin2, lutbackcount4, lutiso) are dumped from MATLAB R2025b directly into a generated header. tol=0 bit-exact on 23 fingerprints covering: 13 single-LUT operations (dilate / erode / bridge / clean / diag / fill / hbreak / majority / perim4 / perim8 / remove / endpoints / fatten) + 4 composite (open / close / bothat / tophat) + 6 iterated (skel∞ / thin∞ / thicken / spur / shrink∞ / branchpoints). Inputs: 20×20 logical random matrix from rng(0). Octave 11.1.0's bwmorph is in the image package — different implementation, not bit-compared."
}
