{
  "name": "prepareSurfaceData",
  "namespace": "stats",
  "setup": "[X, Y] = meshgrid(1:3, 1:2); Z = X + Y; Z(1,2) = NaN;",
  "expr": "[xo, yo, zo] = prepareSurfaceData(X, Y, Z);",
  "iters": 5,
  "tol": 1e-15,
  "fingerprint": [
    "size(xo, 1)",
    "size(xo, 2)",
    "sum(xo)",
    "sum(yo)",
    "sum(zo)"
  ],
  "comment": "Sig: [xo, yo, zo] = prepareSurfaceData(X, Y, Z). Linearises (column-major) and drops rows where any of x, y, z is NaN/Inf. Returns column vectors."
}
