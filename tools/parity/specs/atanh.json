{
  "name": "atanh",
  "namespace": "core",
  "setup": "x = linspace(-0.99, 0.99, 1000000);",
  "expr": "y = atanh(x);",
  "iters": 20,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: Y = atanh(X). 1M-pt on (-1,1) (avoid pole). 20 iters. Element-wise SAVE."
}
