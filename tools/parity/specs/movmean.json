{
  "name": "movmean",
  "namespace": "core",
  "setup": "x = linspace(0, 1, 1000000); k = 5;",
  "expr": "y = movmean(x, k);",
  "iters": 100,
  "tol": 1e-10,
  "comment": "Sig: M = movmean(X, K). 1M-pt window=5. 100 iters. Element-wise SAVE.",
  "out_var": "y"
}
