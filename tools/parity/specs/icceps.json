{
  "name": "icceps",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 1024)); cx = cceps(x);",
  "expr": "y = icceps(cx);",
  "iters": 100,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: Y = icceps(C). Inverse complex cepstrum. 100 iters."
}
