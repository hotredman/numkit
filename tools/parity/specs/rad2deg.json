{
  "name": "rad2deg",
  "namespace": "core",
  "setup": "x = linspace(-4*pi, 4*pi, 1000000);",
  "expr": "y = rad2deg(x);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: D = rad2deg(R). 1M-pt sweep. 20 iters."
}
