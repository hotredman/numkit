{
  "name": "interp",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 25000));",
  "expr": "y = interp(x, 4);",
  "iters": 100,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: Y = interp(X, L). Upsample\u00d74 with FIR. 100 iters."
}
