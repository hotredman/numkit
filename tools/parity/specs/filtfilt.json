{
  "name": "filtfilt",
  "namespace": "core",
  "setup": "b = ones(1, 5) / 5; a = 1; x = sin(linspace(0, 100, 10000));",
  "expr": "y = filtfilt(b, a, x);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: Y = filtfilt(B, A, X). Zero-phase forward+back. 100 iters.",
  "out_var": "y"
}
