{
  "name": "ksdensity",
  "namespace": "stats",
  "setup": "x = [1 2 3 4 5]'; pts = [0 1 2 3 4 5 6];",
  "expr": "[f, xi] = ksdensity(x, pts, 'Bandwidth', 1);",
  "iters": 5,
  "tol": 1e-9,
  "fingerprint": [
    "f(1)",
    "f(2)",
    "f(3)",
    "f(4)",
    "f(end)",
    "xi(1)",
    "xi(end)"
  ],
  "comment": "Sig: [f, xi, bw] = ksdensity(x[, pts, 'Bandwidth', bw]). Gaussian kernel density estimate at user-specified pts (or 100-point auto grid). With explicit bw the kernel formula matches MATLAB exactly; the auto-bw heuristic uses Silverman's rule which differs slightly from MATLAB's internal."
}
