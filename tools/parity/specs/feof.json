{
  "name": "feof",
  "namespace": "core",
  "setup": "fname = tempname; fid = fopen(fname, 'w'); fwrite(fid, 1:10, 'double'); fclose(fid);",
  "expr": "fid = fopen(fname, 'r'); fread(fid, 100, 'double'); y = feof(fid); fclose(fid);",
  "iters": 1000,
  "tol": 1e-09,
  "comment": "Sig: TF = feof(FID). 1000 iters.",
  "fingerprint": [
    "double(y)"
  ]
}
