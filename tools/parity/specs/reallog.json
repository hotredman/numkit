{
  "name": "reallog",
  "namespace": "core",
  "setup": "x = linspace(0.001, 100, 1000000);",
  "expr": "y = reallog(x);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = reallog(X). Strict positive domain. 1M-pt on [0.001, 100]. 20 iters. Element-wise SAVE."
}
