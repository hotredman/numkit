{
  "name": "log1p",
  "namespace": "core",
  "setup": "x = linspace(-0.5, 5, 1000000);",
  "expr": "y = log1p(x);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = log1p(X) = log(1+X). 1M-pt on [-0.5, 5] (avoid X=-1). 20 iters. Element-wise SAVE."
}
