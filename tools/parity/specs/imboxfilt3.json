{
  "name": "imboxfilt3",
  "namespace": "image",
  "setup": "rng(0); V = rand(7,7,5);",
  "expr": "y = imboxfilt3(V, 3);",
  "iters": 5,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: J = imboxfilt3(V [, FilterSize]). 3-D box (mean) filter over a volume, replicate boundary on all 3 axes. MATLAB R2020a+; Octave-image doesn't have it → correctness=N/A."
}
