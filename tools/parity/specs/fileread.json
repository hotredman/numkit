{
  "name": "fileread",
  "namespace": "io",
  "setup": "",
  "expr": "fname = tempname; fid = fopen(fname, 'w'); fprintf(fid, 'abc'); fclose(fid); s = fileread(fname); delete(fname); r1 = double(strlength(s));",
  "iters": 1,
  "tol": 1e-12,
  "fingerprint": [
    "r1"
  ],
  "comment": "Side-effect smoke test (file I/O round-trip in single snippet via tempname). fileread returns full file content -- 3 chars."
}
