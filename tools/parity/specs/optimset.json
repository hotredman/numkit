{
  "name": "optimset",
  "namespace": "core",
  "setup": "% nop",
  "expr": "y = optimset('Display','off','TolX',1e-6);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: O = optimset('NAME', VAL, ...). 10000 iters.",
  "fingerprint": [
    "double(numel(fieldnames(y)))"
  ]
}
