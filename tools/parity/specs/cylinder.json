{
  "name": "cylinder",
  "namespace": "graphics",
  "setup": "",
  "expr": "[x, y, z] = cylinder(20); r1 = double(numel(x));",
  "iters": 5,
  "tol": 1e-12,
  "fingerprint": ["r1"],
  "comment": "Sig: [X,Y,Z] = cylinder([R, n]). Bit-identical with MATLAB R2025b when called with explicit parens. KNOWN ENGINE GAP: cylinder() vs cylinder (no parens) -- parenless multi-output assignment segfaults numkit; that's a core parser/dispatcher issue, not a toolboxes/cylinder bug. Documented in BUGS.md."
}
