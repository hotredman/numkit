{
  "name": "stdfilt",
  "namespace": "image",
  "setup": "G = [-1 2 7; -5 2 8; -7 pi 9];",
  "expr": "y = stdfilt(G);",
  "iters": 5,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: S = stdfilt(I [, domain]). Local sample std (N-1 norm). Default 3x3 ones, symmetric pad. Uses Octave-source test vector G."
}
