{
  "name": "factor",
  "namespace": "core",
  "setup": "x = 1:1000;",
  "expr": "y = arrayfun(@(n) length(factor(n)), x);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: F = factor(N). Sum of #factors for 1..1000. 100 iters.",
  "fingerprint": [
    "double(sum(y(:)))",
    "double(y(1))",
    "double(y(end))"
  ]
}
