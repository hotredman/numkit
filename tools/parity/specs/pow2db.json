{
  "name": "pow2db",
  "namespace": "core",
  "setup": "x = linspace(0.001, 1000, 100000);",
  "expr": "y = pow2db(x);",
  "iters": 1000,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: D = pow2db(P). 100k iters."
}
