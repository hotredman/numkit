{
  "name": "isfield",
  "namespace": "core",
  "setup": "s = struct('alpha',1,'beta',2,'gamma',3); pat = 'beta';",
  "expr": "y = isfield(s, pat);",
  "iters": 1000,
  "tol": 1e-10,
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ],
  "comment": "Sig: TF = isfield(S, F). 100k iters."
}
