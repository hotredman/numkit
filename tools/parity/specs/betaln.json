{
  "name": "betaln",
  "namespace": "core",
  "setup": "z = linspace(1, 10, 10000); w = linspace(1, 10, 10000);",
  "expr": "y = betaln(z, w);",
  "iters": 1000,
  "tol": 1e-09,
  "comment": "Sig: Y = betaln(Z, W). Element-wise. 100 iters.",
  "out_var": "y"
}
