{
  "name": "acos",
  "namespace": "core",
  "setup": "x = linspace(-1, 1, 1000000);",
  "expr": "y = acos(x);",
  "iters": 20,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: Y = acos(X). 1M-pt sweep on [-1, 1]. 20 iters. Element-wise SAVE."
}
