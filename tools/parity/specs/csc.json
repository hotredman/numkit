{
  "name": "csc",
  "namespace": "core",
  "setup": "x = linspace(0.1, 3.0, 1000000);",
  "expr": "y = csc(x);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = csc(X). 1M-pt on (0, \u03c0). 20 iters."
}
