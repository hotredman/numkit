{
  "name": "median",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 1000000));",
  "expr": "y = median(x);",
  "iters": 100,
  "tol": 1e-10,
  "comment": "Sig: M = median(X). 1M-pt full sort + middle. 50 iters. Scalar fp.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
