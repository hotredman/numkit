{
  "name": "stretchlim",
  "namespace": "image",
  "setup": "I = uint8(reshape(0:99, [10 10]));",
  "expr": "y = stretchlim(I);",
  "iters": 5,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig + small deterministic input. Auto-generated for parity sweep. KNOWN: percentile interpolation differs (numkit uses bin counts, MATLAB uses linear interpolation between bin edges)."
}
