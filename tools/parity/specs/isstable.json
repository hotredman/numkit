{
  "name": "isstable",
  "namespace": "core",
  "setup": "b = [1]; a = [1 -0.5];",
  "expr": "y = isstable(b, a);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: TF = isstable(B, A). 10000 iters.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
