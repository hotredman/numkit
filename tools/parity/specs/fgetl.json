{
  "name": "fgetl",
  "namespace": "core",
  "setup": "fname = tempname; fid = fopen(fname, 'w'); fprintf(fid, 'first line\\nsecond line\\n'); fclose(fid);",
  "expr": "fid = fopen(fname, 'r'); y = fgetl(fid); fclose(fid);",
  "iters": 1000,
  "tol": 1e-09,
  "comment": "Sig: LINE = fgetl(FID). 1000 iters.",
  "fingerprint": [
    "double(strlength(y))"
  ]
}
