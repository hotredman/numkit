{
  "name": "floor",
  "namespace": "core",
  "setup": "x = linspace(-100, 100, 1000000) + 0.3;",
  "expr": "y = floor(x);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = floor(X). 1M-pt sweep with non-integer offset. 20 iters. Element-wise SAVE."
}
