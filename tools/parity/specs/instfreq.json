{
  "name": "instfreq",
  "namespace": "signal",
  "setup": "fs = 1000; t = (0:1/fs:1)'; x = sin(2*pi*100*t);",
  "expr": "f = instfreq(x, fs); r1 = double(numel(f) > 0);",
  "iters": 5,
  "tol": 1e-09,
  "fingerprint": ["r1"],
  "comment": "Sig: f = instfreq(x, fs). Spec-extension batch 2026-05-09 (cycle 40)."
}
