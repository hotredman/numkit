{
  "name": "textscan",
  "namespace": "core",
  "setup": "fname = tempname; fid = fopen(fname, 'w'); fprintf(fid, '1.5 2.5 3.5\\n4.5 5.5 6.5\\n'); fclose(fid);",
  "expr": "fid = fopen(fname, 'r'); y = textscan(fid, '%f %f %f'); fclose(fid);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: C = textscan(FID, FMT). 100 iters.",
  "fingerprint": [
    "double(numel(y))"
  ]
}
