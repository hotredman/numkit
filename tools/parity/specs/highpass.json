{
  "name": "highpass",
  "namespace": "core",
  "setup": "fs = 1000; x = sin(linspace(0, 100, 10000));",
  "expr": "y = highpass(x, 100, fs);",
  "iters": 100,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: Y = highpass(X, FPASS, FS). 100 iters."
}
