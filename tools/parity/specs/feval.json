{
  "name": "feval",
  "namespace": "core",
  "setup": "f = @sin; xv = pi/2;",
  "expr": "y = feval(f, xv);",
  "iters": 1000,
  "tol": 1e-09,
  "comment": "Sig: V = feval(F, X). Call sin(pi/2) via feval. 100k iters.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
