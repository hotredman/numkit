{
  "name": "atan",
  "namespace": "core",
  "setup": "x = linspace(-10, 10, 1000000);",
  "expr": "y = atan(x);",
  "iters": 20,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: Y = atan(X). 1M-pt sweep on [-10, 10]. 20 iters. Element-wise SAVE."
}
