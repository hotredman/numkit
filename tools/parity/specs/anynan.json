{
  "name": "anynan",
  "namespace": "core",
  "setup": "x = linspace(0, 1, 1000000); x(500000) = NaN;",
  "expr": "y = anynan(x);",
  "iters": 50,
  "tol": 1e-10,
  "comment": "Sig: TF = anynan(X). Returns scalar. 100k iters.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
