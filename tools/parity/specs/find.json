{
  "name": "find",
  "namespace": "core",
  "setup": "x = mod(1:1000000, 13) == 0;",
  "expr": "y = find(x);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: K = find(X). 1M-pt logical, ~77k matches. 100 iters.",
  "out_var": "y"
}
