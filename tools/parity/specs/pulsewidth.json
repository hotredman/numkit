{
  "name": "pulsewidth",
  "namespace": "signal",
  "setup": "x = repmat([zeros(1,5) ones(1,5)], 1, 4);",
  "expr": "w = pulsewidth(x); r1 = double(numel(w) > 0);",
  "iters": 5,
  "tol": 1e-09,
  "fingerprint": ["r1"],
  "comment": "Sig: w = pulsewidth(x). Spec-extension batch 2026-05-09 (cycle 40)."
}
