{
  "name": "min",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 1000000));",
  "expr": "y = min(x); e0 = min([]); ne0 = numel(e0); ec = min(zeros(0,3)); er = min(zeros(3,0)); b1 = min(5, NaN); b2 = min(NaN, 5); bn = double(isnan(min(NaN, NaN))); va = min([NaN 6 8],[5 7 NaN]); va1 = va(1); va3 = va(3); [rv, ri] = min([NaN 3 1]); an = double(isnan(min([NaN NaN]))); mc = min([NaN 2; 3 NaN]); mc1 = mc(1); mc2 = mc(2);",
  "iters": 100,
  "tol": 1e-10,
  "comment": "Sig: M = min(X). 1M-pt. 100 iters. Scalar fp. DEEP-PROBE 2026-05-29: min of an EMPTY array returns empty and never errors. Two-arg min(a,b) IGNORES NaN. DEEP-PROBE 2026-06-04: the REDUCTION form also defaults to 'omitnan' like MATLAB — min([NaN 3 1])=1 with idx 3 (was NaN-order-dependent); all-NaN -> NaN; per-column on matrices. va=min([NaN 6 8],[5 7 NaN])=[5 6 8].",
  "fingerprint": [
    "double(y)",
    "numel(y)",
    "ne0",
    "size(e0,1)",
    "size(e0,2)",
    "size(ec,1)",
    "size(ec,2)",
    "size(er,1)",
    "size(er,2)",
    "b1",
    "b2",
    "bn",
    "va1",
    "va3",
    "rv",
    "ri",
    "an",
    "mc1",
    "mc2"
  ],
  "bench_setup": "xb = mod((1:N)*7919,1000)+0.5;",
  "bench_expr": "rb = min(xb);"
}
