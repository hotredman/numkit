{
  "name": "bitcmp",
  "namespace": "core",
  "setup": "a = double(1:1000000);",
  "expr": "y = bitcmp(a, 'uint32');",
  "iters": 50,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = bitcmp(A, type). 1M double + 'uint32' (numkit rejects uint32 array — see BUGS #13). 50 iters."
}
