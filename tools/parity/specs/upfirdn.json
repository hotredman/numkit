{
  "name": "upfirdn",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 1000)); h = ones(1, 5) / 5;",
  "expr": "y = upfirdn(x, h, 3, 2);",
  "iters": 100,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: Y = upfirdn(X, H, P, Q). 100 iters."
}
