{
  "name": "gaussdesign",
  "namespace": "comm",
  "setup": "BT = 0.3; sp = 4; sps = 8;",
  "expr": "h = gaussdesign(BT, sp, sps);",
  "iters": 5,
  "tol": 1e-12,
  "out_var": "h",
  "comment": "Sig: h = gaussdesign(BT, span, sps). Gaussian FIR pulse-shaping filter, length span*sps+1, sum-normalized to 1. Element-wise SAVE."
}
