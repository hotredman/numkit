{
  "name": "mean",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 1000000));",
  "expr": "y = mean(x);",
  "iters": 100,
  "tol": 1e-10,
  "comment": "Sig: M = mean(X). 1M-pt sin reduction. 100 iters. Scalar fp.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
