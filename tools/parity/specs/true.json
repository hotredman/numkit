{
  "name": "true",
  "namespace": "builtin",
  "setup": "",
  "expr": "r1 = double(true); t2 = true(2,2); r2 = double(t2(1)); n2 = numel(t2); t3 = true(3); r3 = double(t3(1)); n3 = numel(t3);",
  "iters": 5,
  "tol": 1e-12,
  "fingerprint": [
    "r1",
    "r2",
    "n2",
    "r3",
    "n3"
  ],
  "comment": "Sig: r = true(...). true()=1; true(2,2) is a 2x2 logical one matrix (numel 4); true(3) is 3x3 (numel 9), every element 1. Indexing goes through a temp var (t2(1)) because MATLAB rejects chained indexing on a call result -- true(2,2)(1) is 'Invalid array indexing' in MATLAB (numkit allows it). namespace=builtin."
}
