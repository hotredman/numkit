{
  "name": "overshoot",
  "namespace": "signal",
  "setup": "x = [zeros(1,5) ones(1,5)*1.2 ones(1,5)];",
  "expr": "os = overshoot(x); r1 = double(numel(os) > 0);",
  "iters": 5,
  "tol": 1e-09,
  "fingerprint": ["r1"],
  "comment": "Sig: os = overshoot(x). Spec-extension batch 2026-05-09 (cycle 40)."
}
