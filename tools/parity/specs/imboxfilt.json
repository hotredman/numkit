{
  "name": "imboxfilt",
  "namespace": "image",
  "setup": "I = magic(5);",
  "expr": "B = imboxfilt(I); B5 = imboxfilt(I, 5);",
  "iters": 5,
  "tol": 1e-09,
  "fingerprint": [
    "size(B,1)",
    "B(1,1)",
    "B(3,3)",
    "B(5,5)",
    "sum(B(:))",
    "B5(3,3)",
    "B5(1,1)"
  ],
  "comment": "MATLAB imboxfilt — local mean (replicate boundary, integral-image). Pins filtered values on magic(5) for default 3x3 and a 5x5 box. Bit-equal MATLAB R2025b (tol=1e-9)."
}
