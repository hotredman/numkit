{
  "name": "zeros",
  "namespace": "builtin",
  "setup": "",
  "expr": "A = zeros(2, 3); r1 = numel(A); r2 = A(1,1); r3 = A(2,3); r4 = sum(A(:)); r5 = numel(zeros(-1, 3)); r6 = numel(zeros(2, 3, -1));",
  "iters": 5,
  "tol": 1e-12,
  "fingerprint": [
    "r1",
    "r2",
    "r3",
    "r4",
    "r5",
    "r6"
  ],
  "comment": "Sig: r = zeros(...). Spec-extension batch 2026-05-09. + negative-dim clamp-to-0 edge (r5=numel(zeros(-1,3))=0, r6=numel(zeros(2,3,-1))=0; bugs/lang/zeros-size-args.md). Non-integer dim errors on both engines (gtest, not a value fingerprint)."
}
