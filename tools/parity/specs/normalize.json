{
  "name": "normalize",
  "namespace": "stats",
  "setup": "x = [1 2 3 4 5];",
  "expr": "r = normalize(x,'range',[0 10]); n1 = normalize(x,'norm',1); ni = normalize(x,'norm',Inf); sf = normalize(x,'scale','first'); cm = normalize(x,'center','median');",
  "iters": 3,
  "tol": 1e-12,
  "fingerprint": [
    "r(1)", "r(3)", "r(5)",
    "n1(1)", "n1(5)",
    "ni(1)", "ni(5)",
    "sf(5)",
    "cm(1)", "cm(5)"
  ],
  "comment": "Sig normalize(A,method,param). The method PARAMETER was parsed-and-ignored: 'range' bounds [lo hi] (default [0 1]); 'norm' exponent p (1/2/Inf); 'scale' divisor 'std'(def)/'first'/'iqr'/'mad' or numeric; 'center' 'mean'(def)/'median' or numeric. normalize([1..5],'range',[0 10]) -> [0 2.5 5 7.5 10]; 'norm',1 -> /15; 'norm',Inf -> /5; 'scale','first' -> /x(1)=[1..5]; 'center','median' -> [-2 -1 0 1 2]. numkit previously ignored the param -- now honored. Matches MATLAB R2025b."
}
