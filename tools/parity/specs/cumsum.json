{
  "name": "cumsum",
  "namespace": "core",
  "setup": "x = linspace(0, 1, 1000000);",
  "expr": "y = cumsum(x);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = cumsum(X). 1M-pt cumulative sum (default dim). 20 iters."
}
