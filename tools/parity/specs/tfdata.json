{
  "name": "tfdata",
  "namespace": "control",
  "setup": "sys = tf([1 2], [1 3 2]);",
  "expr": "[num, den] = tfdata(sys, 'v');",
  "iters": 5,
  "tol": 1e-12,
  "fingerprint": [
    "num(1)",
    "num(2)",
    "num(3)",
    "den(1)",
    "den(2)",
    "den(3)"
  ],
  "comment": "Sig: [num, den] = tfdata(sys[, 'v']). Extracts numerator/denominator coefficient vectors. With 'v' returns numeric row vectors; pads num with leading zeros so length matches den. Accepts tf / zpk / ss inputs."
}
