{
  "name": "noisebw",
  "namespace": "comm",
  "setup": "",
  "expr": "bw = noisebw([0.5 0.5], [1], 1024, 1000); r1 = double(bw);",
  "iters": 5,
  "tol": 1.0,
  "fingerprint": ["r1"],
  "comment": "Sig: bw = noisebw(num, den, Nsamp, fs). Equivalent noise bandwidth via NBW = (fs/N) * sum(|H|^2) / max(|H|^2). Matches MATLAB R2025b within ~0.5 Hz on probed FIR (numerical-grid difference)."
}
