{
  "name": "pcacov",
  "namespace": "stats",
  "setup": "X = [1 2 3; 2 1 5; 3 4 1; 5 6 2; 7 5 4]; C = cov(X);",
  "expr": "[coeff, latent, explained] = pcacov(C);",
  "iters": 5,
  "tol": 1e-9,
  "fingerprint": [
    "abs(coeff(1,1))", "abs(coeff(2,2))", "abs(coeff(3,3))",
    "latent(1)", "latent(2)", "latent(3)",
    "explained(1)", "explained(2)", "explained(3)"
  ],
  "comment": "Sig: [coeff, latent, explained] = pcacov(C). Like pca but on a precomputed covariance matrix. Bit-identical to MATLAB R2025b."
}
