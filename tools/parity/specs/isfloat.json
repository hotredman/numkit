{
  "name": "isfloat",
  "namespace": "core",
  "setup": "x = linspace(0, 1, 1000000);",
  "expr": "y = isfloat(x);",
  "iters": 50,
  "tol": 1e-10,
  "comment": "Sig: TF = isfloat(X). Returns scalar. 100k iters.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
