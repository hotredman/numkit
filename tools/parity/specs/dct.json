{
  "name": "dct",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 1024));",
  "expr": "y = dct(x);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: Y = dct(X). 1024-pt DCT. 1000 iters.",
  "out_var": "y"
}
