{
  "name": "entropy",
  "namespace": "image",
  "setup": "I = uint8(reshape(0:99, [10 10]));",
  "expr": "y = entropy(I);",
  "iters": 5,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: E = entropy(I [, nbins]). Shannon entropy in bits over imhist of im2uint8(I) (256 bins by default). Octave-image has entropy."
}
