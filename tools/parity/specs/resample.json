{
  "name": "resample",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 10000));",
  "expr": "y = resample(x, 3, 2);",
  "iters": 100,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: Y = resample(X, P, Q). 3:2. 100 iters."
}
