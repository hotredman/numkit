{
  "name": "load",
  "namespace": "builtin",
  "setup": "",
  "expr": "r1 = 1;",
  "iters": 1,
  "tol": 1e-12,
  "fingerprint": ["r1"],
  "comment": "DEFERRED — `load` round-trip with tempname can't be expressed in a single-snippet parity spec (setup tempname differs from expr tempname; engines diverge on file I/O sandbox). Functionality validated in gtest/smoke instead. Placeholder spec; KNOWN GAP — see audit/closed/builtin/load.md."
}
