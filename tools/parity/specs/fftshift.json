{
  "name": "fftshift",
  "namespace": "core",
  "setup": "X = fft(sin(linspace(0, 100, 1024)));",
  "expr": "y = fftshift(X);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: Y = fftshift(X). 1024-pt shift. 1000 iters.",
  "fingerprint": [
    "double(real(sum(y(:))))",
    "double(real(y(1)))",
    "double(real(y(end)))"
  ]
}
