{
  "name": "assert",
  "namespace": "core",
  "setup": "% nop",
  "expr": "assert(1 == 1); y = 'ok';",
  "iters": 1000,
  "tol": 1e-09,
  "comment": "Sig: assert(COND). Pass-case. 100k iters.",
  "fingerprint": [
    "double(strlength(y))"
  ]
}
