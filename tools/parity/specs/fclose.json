{
  "name": "fclose",
  "namespace": "core",
  "setup": "fname = tempname; fid = fopen(fname, 'w'); fwrite(fid, 1:10, 'double'); fclose(fid);",
  "expr": "fid = fopen(fname, 'r'); y = fclose(fid);",
  "iters": 1000,
  "tol": 1e-09,
  "comment": "Sig: STATUS = fclose(FID). 1000 iters.",
  "fingerprint": [
    "double(y)"
  ]
}
