{
  "name": "cot",
  "namespace": "core",
  "setup": "x = linspace(0.1, 3.0, 1000000);",
  "expr": "y = cot(x);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = cot(X). 1M-pt on (0, \u03c0) avoiding 0/\u03c0 poles. 20 iters."
}
