{
  "name": "imreconstruct",
  "namespace": "image",
  "setup": "mask = false(7, 7); mask(2:6, 2:6) = true; mask(4, 4) = false; marker = false(7, 7); marker(3, 3) = true;",
  "expr": "y = double(imreconstruct(marker, mask));",
  "iters": 5,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: J = imreconstruct(marker, mask). Reconstruction by dilation; marker grows to fill the connected mask region."
}
