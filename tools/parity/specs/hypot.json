{
  "name": "hypot",
  "namespace": "core",
  "setup": "a = linspace(-3, 3, 1000); b = linspace(-3, 3, 1000); [A, B] = meshgrid(a, b);",
  "expr": "y = hypot(A, B);",
  "iters": 20,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: Y = hypot(A, B). 1k\u00d71k grid. 20 iters. Element-wise SAVE."
}
