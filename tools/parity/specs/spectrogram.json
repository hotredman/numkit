{
  "name": "spectrogram",
  "namespace": "core",
  "setup": "x = chirp(linspace(0, 1, 4096), 50, 1, 250);",
  "expr": "[y, jj, kk] = spectrogram(x, 256);",
  "iters": 100,
  "tol": 1e-09,
  "out_var": "y",
  "comment": "Sig: [S, F, T] = spectrogram(X, NFFT). 100 iters. SAVE on S magnitude."
}
