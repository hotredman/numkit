{
  "name": "diff",
  "namespace": "core",
  "setup": "x = linspace(0, 100, 1000000);",
  "expr": "y = diff(x);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = diff(X). 1M-pt adjacent differences. 20 iters. Element-wise SAVE."
}
