{
  "name": "rssq",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 1000000));",
  "expr": "y = rssq(x);",
  "iters": 100,
  "tol": 1e-09,
  "comment": "Sig: R = rssq(X). 100 iters.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
