{
  "name": "movsum",
  "namespace": "core",
  "setup": "x = linspace(0, 1, 1000000); k = 5;",
  "expr": "y = movsum(x, k);",
  "iters": 20,
  "tol": 1e-10,
  "out_var": "y",
  "comment": "Sig: Y = movsum(X, K). 1M-pt moving window K=5. 20 iters. Element-wise SAVE."
}
