{
  "name": "im2col",
  "namespace": "image",
  "setup": "A = reshape(1:16, [4 4]);",
  "expr": "y = im2col(A, [2 2], 'sliding');",
  "iters": 5,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: B = im2col(A, [m n], 'sliding'). 4x4 lattice → 4x9 (3·3 sliding positions, column-major within block)."
}
