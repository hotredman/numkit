{
  "name": "dot",
  "namespace": "core",
  "setup": "a = sin(linspace(0, 10, 1000000)); b = cos(linspace(0, 10, 1000000));",
  "expr": "y = dot(a, b);",
  "iters": 100,
  "tol": 1e-10,
  "comment": "Sig: D = dot(A, B). 1M-elem dot product. 100 iters. Scalar fp.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
