{
  "name": "tanh",
  "namespace": "core",
  "setup": "x = linspace(-5, 5, 1000000);",
  "expr": "y = tanh(x);",
  "iters": 20,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: Y = tanh(X). 1M-pt sweep on [-5, 5]. 20 iters. Element-wise SAVE."
}
