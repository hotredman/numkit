{
  "name": "who",
  "namespace": "builtin",
  "setup": "",
  "expr": "r1 = 1;",
  "iters": 1,
  "tol": 1e-12,
  "fingerprint": ["r1"],
  "comment": "DEFERRED — numkit's `who` operates as command-form only (prints names) and does not return a cell array via `names = who`. Placeholder spec; KNOWN GAP — see audit/closed/builtin/who.md."
}
