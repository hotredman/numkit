{
  "name": "brighten",
  "namespace": "image",
  "setup": "M = repmat(linspace(0, 1, 8)', 1, 3);",
  "expr": "y = [brighten(M, 0.6); brighten(M, -0.4); brighten(M, 0.0)];",
  "iters": 5,
  "tol": 1e-12,
  "out_var": "y",
  "comment": "Sig: rmap = brighten(map, beta). Output = map .^ gamma where gamma = 1-beta if beta>0 else 1/(1+beta). MATLAB+Octave both ship brighten."
}
