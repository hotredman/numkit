{
  "name": "max",
  "namespace": "core",
  "setup": "x = sin(linspace(0, 100, 1000000));",
  "expr": "y = max(x);",
  "iters": 100,
  "tol": 1e-10,
  "comment": "Sig: M = max(X). 1M-pt. 100 iters. Scalar fp.",
  "fingerprint": [
    "double(y)",
    "numel(y)"
  ]
}
