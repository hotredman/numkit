# Third-party code never enters git — cloned by fetch.py on demand.
corpus/
# Transient harness state.
candidates.json
__pycache__/
# Transient console transcripts (shell redirections) — the durable record is reports/.
*.log
